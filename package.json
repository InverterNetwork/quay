{
  "name": "quay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "quay": "bun run src/cli/index.ts",
    "embed": "bun run scripts/embed.ts",
    "build": "bun run scripts/embed.ts && bun build --compile --no-compile-autoload-bunfig --no-compile-autoload-dotenv src/cli/index.ts --outfile dist/quay",
    "prepare": "bun run scripts/embed.ts"
  },
  "bin": {
    "quay": "src/cli/index.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
